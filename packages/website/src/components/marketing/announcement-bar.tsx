import { SparkleIcon } from './icons';

export function AnnouncementBar() {
	return (
		<div className="announce">
			<SparkleIcon size={16} />
			Every account includes a free onboarding specialist — a real human who sets it all up for you.
		</div>
	);
}
