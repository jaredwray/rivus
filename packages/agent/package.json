{
	"name": "@rivus/agent",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Rivus AI agent — a Cloudflare Agent (Durable Object) the app chats with.",
	"license": "MIT",
	"scripts": {
		"dev": "wrangler dev",
		"build": "wrangler deploy --dry-run --outdir dist",
		"deploy": "wrangler deploy",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage",
		"type-check": "tsc --noEmit",
		"clean": "rm -rf dist coverage .wrangler"
	},
	"dependencies": {
		"agents": "^0.16.1",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "catalog:",
		"@faker-js/faker": "catalog:",
		"@vitest/coverage-v8": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:",
		"wrangler": "catalog:"
	}
}
