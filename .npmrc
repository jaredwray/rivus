# Most pnpm settings live in pnpm-workspace.yaml (pnpm 10+).
# Resolve peer dependencies automatically so workspaces stay explicit-but-ergonomic.
auto-install-peers=true
# Keep the lockfile authoritative; CI uses --frozen-lockfile.
prefer-frozen-lockfile=true
