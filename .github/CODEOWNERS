# High-risk paths. Last matching pattern wins.
# Root-anchored so nested copies (e.g. skills/**/scripts/) are not owned here.
# `*` keeps review on every other path as well.
*              @jaredwray
/.github/      @jaredwray
/.cursor/      @jaredwray
/.devcontainer/ @jaredwray
/scripts/      @jaredwray
